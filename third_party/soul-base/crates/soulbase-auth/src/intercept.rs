//! Reserved for integration helpers with soulbase-interceptors.
//! This module will host protocol-specific glue code that extracts request context,
//! calls the auth facade, and maps decisions back into interceptor envelopes.
