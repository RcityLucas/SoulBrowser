# Copy to config/local.env and fill in your own secrets
#
# Required when running planner/CLI flows that hit OpenAI.
SOULBROWSER_OPENAI_API_KEY=sk-your-api-key

# Uncomment these lines to force the CDP adapter to boot real Chrome instead of the stub
# transport. Adjust the path/profile/flags for your environment.
# SOULBROWSER_USE_REAL_CHROME=1
# SOULBROWSER_CHROME=/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome
# SOULBROWSER_CHROME_PROFILE=./.soulbrowser-profile
# SOULBROWSER_CHROME_ARGS=--headless=new --remote-allow-origins=* --disable-gpu
